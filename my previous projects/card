import random

class Card:
    def __init__(self, rank, value, suit):
        self.rank = rank
        self.value = value
        self.suit = suit

    def display_card(self):
        print("Card rank : ", self.rank, '\n', " Card suit : ", self.suit)
